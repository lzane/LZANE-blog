image: monachus/hugo
variables:
  GIT_SUBMODULE_STRATEGY: recursive

stages:
    - gitlabPages
    # - deploy

# deploy:
#     stage: deploy
#     script:
#         - hugo -t cocoa
#     only:
#         - master

pages:
    stage: gitlabPages
    script:
        - hugo -t cocoa --baseURL="https://lzane.gitlab.io/blog"
    artifacts:
        paths:
            - public
    only:
        - master
